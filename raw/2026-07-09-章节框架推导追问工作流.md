从第 4 章开始，围绕 `训练目标` 这一章的搭建，形成了一段可复用的追问式工作流。

这段过程不是直接写正文，而是连续追问：

```text
下一章怎么说？
你真的理解训练目标是什么吗？
为什么需要先说训练目标？
这句话是不是说得太绝对？
训练目标是不是一层抽象锚点，而不是训练最终形态？
现在直接写是不是浅学习？
章节 4 的作用到底是什么？
这样安排有什么用？
节关系应该怎么排？
标题和节奏是否适合人类和 agent 阅读？
少了什么？
哪些节需要合并？
```

过程中形成的关键判断：

```text
004 训练目标不是为了百科式解释“训练目标”这个词。
它的作用是把 003 中的 Agent 行为观察与系统归因，
推进为可组织上下文训练的判断锚点。
```

因此，章节框架不应按概念罗列，而应按问题推进：

```text
从行为归因到训练判断
-> 什么是训练目标
-> 目标与目标描述
-> 训练目标的适用范围
-> 目标与训练结构
-> 从现象中提炼目标
-> 边界与下一步
```

可复用点：

```text
当一个章节看起来可以直接写时，先不要急着扩写。
先追问这一章在前后章节之间承担什么转换功能。
再追问每一节是否真的推进问题，而不是把刚讨论过的概念重新排列。
如果发现只是把共识表层套进结构，就标记为浅学习，退回到章内节框架。
```
