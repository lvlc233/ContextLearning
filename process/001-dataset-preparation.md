# 数据集与准备

在 `上下文学习` 中，数据集的格式和内容没有严格限制。由于大部分 harness 都具备文档处理能力，最简单的数据集可以从一个 Markdown 文件开始。

一个最基础的数据单位是：

- Q：原始上下文
- A：模型输出
- T：人类标注（可选）

## 数据集类型

具体的数据集类型和样本划分，见 [`数据集与样本划分`](../concepts/core/007-dataset-and-sample-partition.md)。

在这个步骤里，重点不是重新定义每一类数据集，而是把原始材料准备出来。您不需要一次性就将准备所有的数据类型与数据，根据自己所需进行调整，这些内容也可以在后续过程中不断扩展，重要的是确保它们能流向后续的目标提炼、标注、训练和测试环节。

## 案例

```text
Q：用户要求 Agent 压缩一份 skill，使它更短，但不能丢失任务边界、失败条件和测试标准。

A：Agent 删除了重复表达，也删除了失败条件和测试标准。

T：人类标注：输出确实更短，但丢失了必要约束，不能视为有效优化。
```

这条记录可以在不同阶段承担不同作用：

- 作为 **原始数据集**：用于观察上下文压缩时常见的失败模式。
- 作为 **标注数据集**：`T` 中的人类判断提供了明确训练信号。
- 作为 **偏好数据集**：它表达了“更短但不能丢失约束”的偏好。
- 作为 **训练目标集** 的来源：可以提炼出“压缩上下文时保留任务边界、失败条件和测试标准”这个目标。
- 作为 **训练数据集**：可以只取 `Q` 作为输入，让新上下文生成输出，再根据目标和标注判断是否改善。


## 技巧

### 使用自己的会话作为数据集

从实际的生产活动中筛选出自己的数据集。

可以根据不同的对话内容，选择不同的数据单位：

- 以一次 run 记录作为数据单位，通常去除工具调用等细节；
- 以一组与指定任务相关的 run 记录作为数据单位；
- 以一个完整会话作为数据单位；
- 以一组输入、输出和人类评价组成一个样本。

后续可以补充会话检索、样本抽取和标注辅助工具。

## 引用

- [`上下文学习`](../concepts/core/001-context-learning.md)
- [`数据集与样本划分`](../concepts/core/007-dataset-and-sample-partition.md)
- [`数据标注`](../concepts/core/003-数据标注.md)
- [`训练信号`](../concepts/core/004-训练信号.md)
- [`训练目标`](../concepts/core/005-training-target.md)

