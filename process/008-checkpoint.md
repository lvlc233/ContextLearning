# checkpoint
该概念取自 模型训练 过程中的 训练结果保存

checkpoint 至少保留：

- 当前上下文版本号(用于标记)
- 上下文本体
- 训练时使用的数据集与输出(用于观测)
- 训练目标偏离程度(类似于loss)

### 使用git作为 checkpoint 存储工具

在进行 "训练" 过程中,可以选择git提交或tag记录每个checkpoint;

如果使用多维度分区训练的方式,可以使用文件夹做内容隔离;

推荐以checkpoint作为单体而非某次训练迭代作为提交单位

