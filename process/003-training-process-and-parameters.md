# 2. 设计训练流程与参数
包括训练迭代方式,轮次,选择的数据集大小,批次,更改量,评估标准等可以理解为"超参数"的内容;(尽管被这么比喻,但实际上,它可以是一套自然语言描述,也可以是一套程序系统,例如loop)

一个好的训练训练过程与参数通常也不是一触即就,相反,它与上下文本身一样,需要不断的调试与迭代;

而一个好的训练流程与参数通常可以复用在许多不同场景的上下文训练过程中,希望您能保留下来属于自己的训练流程与参数

