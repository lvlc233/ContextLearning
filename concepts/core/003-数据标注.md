## 数据标注

数据标注是形成训练方向的一种常见方式。

有别于传统机器学习中的数据标注，上下文学习中的标注可以使用自然语言完成。因为语言模型本身具备自然语言理解能力，所以标注不必只是一组离散标签。

标注可以很简单：

```text
好 / 坏
更好 / 更差
有效 / 无效
```

也可以更具体：

```text
哪一句好；
哪一句不好；
为什么不好；
哪条规则导致了偏移；
哪个版本更接近训练目标；
是否出现副作用。
```

人类偏好也可以存放在数据标注中。它可以表现为对某个输出的直接评价，也可以表现为对两个版本的比较，或者对某种规则方向的接受与拒绝。